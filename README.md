# SAALF
SAALF is a semi-automatic framework for labelling time-series anomalies, combining a confidence-weighted detector ensemble (via ADSFra) with a pattern-aware module that flags sustained deviations from periodic behaviour, to speed up expert validation.
